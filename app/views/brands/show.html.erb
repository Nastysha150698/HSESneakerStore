<p id="notice"><%= notice %></p>

<p>
  <strong>Brand:</strong>
  <%= @brand.brand_id %>
</p>

<p>
  <strong>Title:</strong>
  <%= @brand.title %>
</p>

<%= image_tag @brand.logo %>

<%= link_to 'Edit', edit_brand_path(@brand) %> |
<%= link_to 'Back', brands_path %>
